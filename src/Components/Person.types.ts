export type aboutPersonProps = {
  aboutPerson: {
    name: string;
    age: number | string;
    isAdult: boolean;
    country: string;
  }[];
};

export type HeadingProps = {
  children: string;
};
